# -*- python -*-
# ex: set syntax=python:

#repository descriptions - (url, [local_cache_path]) or ((url, [local_cache_path]), ['user/passwd'])

foo_repository = ('svn://10.10.0.156:6666/foo', '~/.buildc_libs/foo')

bar_repository = ('svn://10.10.0.156:6667/bar', '~/.buildc_libs/bar')

external_repositories = [
                            foo_repository,
                            bar_repository
                        ]
